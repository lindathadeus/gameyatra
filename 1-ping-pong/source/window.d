module window;

// Adding the game window stuff here for better readability
// This module aims to add a window object and then use SDL to
// render on the screen

// Constants related to the window (or screen)
const int SCREEN_WIDTH = 800;
const int SCREEN_HEIGHT = 600;

void create_game_window() {}
